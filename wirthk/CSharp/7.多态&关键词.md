#abstract-抽象  
-抽象方法 抽象属性（抽象getset方法）
-不能实例化，只能继承
-有一个抽象方法，类必须是抽象类
-抽象的方法默认是virtual
![[Pasted image 20260727141850.png]]

抽象方法 - 必须重写( 添加关键词 override ) / 继续抽象
![[Pasted image 20260727142219.png]]

#virtual-必须是已经实现的基类方法
-派生类可以重写( 添加关键词 override)或者不重写
-字段和静态方法不能是virtual
-属性可以是virtual

#new-新的方法 - 少用
全新的方法，只是名字刚好重名，引用类型是什么 就调用哪一个方法
![[Pasted image 20260727150924.png]]

#override-重写 - 函数一模一样
![[Pasted image 20260727151004.png]]
 派生类 转换成 基类，创建什么派生类 就用哪一个派生类的方法，改写了基类的虚方法

#多态
![[Pasted image 20260727154126.png]]






#overload-重载 
派生类和基类 或者同一个类里 实现重载需要添加关键词overload
![[Pasted image 20260727143318.png]]


#seald
![[Pasted image 20260727145548.png]]

#is 
![[Pasted image 20260727161411.png]]

#this - 指向自己的关键词
![[Pasted image 20260725145246.png]]

#base
调用基类的方法

#operator-运算符重载
![[Pasted image 20260727162149.png]]
![[Pasted image 20260727163148.png]]
v1 + v2 == 编译器自动映射 == 类名.operator+(v1, v2)



#static
被 static 修饰的东西，属于【类本身】，不属于【类的实例（对象）】

1.
-普通变量/方法：必须 new创建对象之后才能访问
-静态变量/方法：不需要 new 对象，直接通过 类名.成员访问

2.
-静态变量 - 所有对象共享同一份数据
![[Pasted image 20260724164454.png]]

3.
-非静态方法 -可掉用静态/非静态  变量/方法
-静态方法 -可掉用静态变量/方法
静态方法 和非静态方法 在本身的类内部，可以直接调用方法名使用

4.
-静态构造函数 static Person(){}
只会自动执行一次，第一次访问这个类的任意静态成员之前执行且只执行一次
不能写参数、不能手动调用、不能加访问修饰符（默认 private）- 常用于初始化静态变量、加载配置
作用：
加载配置文件
初始化静态字典、连接参数
注册全局回调

5.
-静态类 static class
不能 new 创建对象
里面所有成员强制要求 static- 典型例子：Console、Math


#const和readonly
-const - 只支持基础类型：数值、bool、char、string、null；
	隐式 static，不用写 static；实例无法访问，只能 类名.常量
	只能在声明接处直赋值
	所有对象都拥有一个常数值
-readonly - 基础类型、struct、class、数组都可以
	声明时初始化
	实例构造函数（实例 readonly） / 静态构造函数（static readonly）- 构造执行完毕后，任何地方不能再次修改
	每一个对象都拥有自己的常数值